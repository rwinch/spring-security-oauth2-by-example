logging:
  level:
    reator:
      netty:
        channel: DEBUG

spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  security:
    user:
      password: password
users-url: http://localhost:8081/users
messages-url: http://localhost:8082/messages